# MO-26-09-02-21.23.41 — reusable research library

- Opened issue #203 and claimed the governed branch from current origin/main.
- Used Lakina's field-tested publisher as the specification, then removed project constants behind
  the `researchLibrary` manifest contract.
- Kept Babel independent at the canonical Docling directory boundary.
- Added the non-overwriting initializer, shared publisher, typed catalog parser, browser byte/hash
  verifier, tests, architecture, and operator runbook.
- Considered archive/upload packages; retained Python stdlib plus the existing gcloud boundary to
  avoid a new dependency without duplicating behavior.
