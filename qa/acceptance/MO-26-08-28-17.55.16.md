# Acceptance — bootstrap runtime dependencies

- The disposable current-main clone installs its frozen reviewed lockfile before Node loads the
  committed CLI.
- The unit harness fails if CLI invocation precedes dependency installation.
- A real bootstrap from a fake pre-`self` binary succeeds with an isolated home, npm prefix,
  registry and auto-update configuration.
- The isolated install matches current Morpheus `main`, registers the project, and preserves
  non-Morpheus Git-hook content while adding both managed hooks.
