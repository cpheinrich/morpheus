# Morpheus technical-health audit — 2026-09-07

Baseline: `76bb3b3973b6` (`origin/main`). Scope: all 897 tracked files, with an exact full code graph,
direct inspection of every reported coverage gap, configuration, tests, dependencies, and live
GitHub Actions state. This refresh reuses audit PR #176; operational remediation remains isolated
in draft PR #177.

## Executive summary

Morpheus's code controls are healthy: lint, typecheck, compile, all 1,102 tests, governance
validation, and the production dependency audit pass. The urgent finding persists: the protected
default branch's scheduled workflow repeatedly fails before any job starts. Draft PR
[#177](https://github.com/cpheinrich/morpheus/pull/177) is refreshed on current main with the
bounded permission repair and immutable third-party action pins. No new cleanup was both useful
and low-risk, so #176 retains its existing lint-contract and identifier cleanup without adding
churn.

## Ranked findings

| Rank | Finding | Evidence | Confidence | Impact | Next action |
|---:|---|---|---|---|---|
| 1 | The main-branch schedule still fails before job creation | Main run [34142869463](https://github.com/cpheinrich/morpheus/actions/runs/34142869463) concluded `startup_failure` with no jobs, continuing the repeated schedule history. | Certain | High: the scheduled control is absent on the protected default branch | Review draft [#177](https://github.com/cpheinrich/morpheus/pull/177); keep it draft until the workflow-policy change is accepted. |
| 2 | CLI parsing and dispatch remain concentrated | The graph reports `main` at cyclomatic complexity 89/cognitive 169 and `parseArgs` at 64/192 in `src/cli/index.ts`, both above the prior audit. | High | Medium: grammar and error-text changes share a large regression surface | Establish a stable invocation/error acceptance seam before splitting; a mechanical extraction is not low-risk. |
| 3 | Helper duplication remains local | Nine similarity relationships are mainly small file, template, and index-rendering helpers; direct inspection found no duplicated business rule. | High | Low: modest maintenance cost, with locality still aiding comprehension | Keep as inventory; extract when a shared behavior contract emerges. |

## Dead code, dependencies, assets, tests, and drift

- Seven uncalled graph candidates were locally used callbacks, template keys, or public exports on
  direct inspection; none justified deletion.
- `pnpm audit --prod` reported zero vulnerabilities across 16 production dependencies.
- No obsolete tracked asset or configuration was established. Committed `dist/` remains an
  intentional repository contract and was regenerated by `compile`.
- Lint, typecheck, 39 test files/1,102 tests, compile, PM validation/index, inbox/team validation,
  and `git diff --check` passed.

## Method and limits

The exact graph was indexed at 2026-09-07T16:23:50Z. Coverage recorded only partial parses in
`src/web/consumer-auth/templates-tests.ts` and `tests/firebase-cli.test.ts`; every reported range
was read directly. A clean result elsewhere means no recorded indexing gap, not proof of absence.
Low-risk work remains in #176; disruptive workflow remediation remains in linked draft #177.

## Resolution — 2026-09-10

Chris requested completion rather than retaining this audit as an open inventory.
The high scheduler/action-pin finding is implemented in #177; the medium CLI split
and low duplicate-helper cleanup are implemented in #234. This audit PR retains the
lint/CI repair and unused-identifier cleanup, with a real ESLint regression test.
Final merge and main-branch scheduler evidence are recorded in the closeout worklog.

The CLI cleanup preserves the existing invocation contract and replaces repetitive
scalar/boolean parsing and monolithic dispatch with tested seams and command-family
handlers. Shared accessibility, optional-read, JSON-discovery, scaffold-write and
markdown-table contracts replace matching implementations. Lookalikes with different
failure policies remain explicit instead of silently changing semantics.
