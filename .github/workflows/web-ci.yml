name: Web CI

# Reusable workflow for a Next.js surface in apps/web.
#
#   jobs:
#     web:
#       uses: cpheinrich/morpheus/.github/workflows/web-ci.yml@main
#       with:
#         working-directory: apps/web

on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        default: "apps/web"
      node-version:
        type: string
        default: "22"
      pnpm-version:
        description: >
          Leave empty to let package.json's `packageManager` field decide, which
          is the modern default. Setting both makes pnpm/action-setup fail with
          "Multiple versions of pnpm specified".
        type: string
        default: ""
      run-e2e:
        type: boolean
        default: false
      build-tokens:
        description: Run the design-token build before the app build.
        type: boolean
        default: true

jobs:
  web:
    runs-on: ubuntu-latest
    # A hung step otherwise runs to GitHub's six-hour default on billed
    # minutes. This ceiling sits far above the honest runtime, so it only ever
    # fires on a hang — which it then reports quickly and legibly.
    timeout-minutes: 15
    # A superseded push must cancel rather than run to completion
    # beside its replacement. Job-level because this file is called,
    # and a called workflow's top-level concurrency does not govern
    # the caller's run.
    concurrency:
      group: web-ci-${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ inputs.pnpm-version }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      # Tokens are generated from hq/brand into packages/shared, so the app
      # cannot typecheck until they exist.
      - name: Build design tokens
        if: ${{ inputs.build-tokens }}
        run: pnpm run build:tokens

      # --if-present so a surface that has not added a script yet is skipped
      # rather than failing. Discovered retrofitting Evo, which had neither
      # typecheck nor test defined.
      - name: Typecheck
        working-directory: ${{ inputs.working-directory }}
        run: pnpm run --if-present typecheck

      - name: Lint
        working-directory: ${{ inputs.working-directory }}
        run: pnpm run --if-present lint

      - name: Unit tests
        working-directory: ${{ inputs.working-directory }}
        run: pnpm run --if-present test

      - name: Build
        working-directory: ${{ inputs.working-directory }}
        run: pnpm build

      - name: E2E
        if: ${{ inputs.run-e2e }}
        run: pnpm exec playwright install --with-deps chromium && pnpm exec playwright test
