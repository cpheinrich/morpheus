# Firebase release action

Run inside the caller's protected environment and whole-publication concurrency group.
The implementation is compiled from `src/firebase-release/` into committed `dist/`; it needs
no application dependency install. See [the contract and examples](../../../docs/runbooks/firebase-releases.md).
