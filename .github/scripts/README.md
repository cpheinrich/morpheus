# Workflow helpers

These scripts execute from the trusted Morpheus checkout. The iOS visual QA publisher
reads caller inventories and Xcode exports strictly as data, and owns only the fixed
`nightly-ios-visual-qa` branch. See architecture.md §18 for the workflow contract.
