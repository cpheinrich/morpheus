// The installer replaces this value in the staged copy. It lets a newly installed
// client retire an idle service that still has the previous package loaded.
export const installationId = "development";
