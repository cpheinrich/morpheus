---
id: MO-035
title: "Reusable checks only worked in the repo that defines them"
status: shipped
priority: P0
goal: MO-G-2026-Q3-01
owner: agent
prs: [28]
created: 2026-07-29
updated: 2026-07-29
---

## Context

_Why this matters._

## Approach

_How it will be done._

## Context

All three retrofit PRs failed both convention checks with:

```
Error: No pnpm version is specified.
```

`pnpm/action-setup` reads `packageManager` from the **repository root**, and `pnpm-version`
defaults to empty so that field decides. But in `pm-check` and `pr-check` the root is the
*consuming* repo — Morpheus lives in `.morpheus/`. Two of the three retrofits have no root
`package.json` at all.

## The real failure

**The workflow was only ever exercised in the repo that defines it.** Morpheus's own root has
`packageManager`, so it passed at home and failed everywhere else, and the bug was invisible until
a project that is not Morpheus used it.

Evo passed too — it is a pnpm workspace. It took a static site and a Python repo to expose it.

## Shipped

`package_json_file: .morpheus/package.json` on both, so the version comes from Morpheus's manifest
rather than whatever the consumer happens to have.

## Related

MO-018 was the same fault in the opposite direction — setting a version *and* having
`packageManager` made action-setup fail with "Multiple versions of pnpm specified". Both come from
the workflow guessing which manifest it means. It should never guess.
