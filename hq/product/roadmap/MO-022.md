---
id: MO-022
title: "Brand package has a declared required set"
status: review
priority: P1
goal: MO-G-2026-Q3-01
owner: agent
prs: []
created: 2026-07-29
updated: 2026-07-29
---

## Context

The generated prompt named four output paths and said nothing about what "done" meant for any of
them. An agent could write a two-colour `tokens.json` and a `visual-system.md` that was still
mostly scaffold, and nothing would object.

Chris's framing: the package should have a required set plus optional parts built out over time.

## Shipped

`src/brand/package.ts` declares both lists once. Three consumers read it — the design-session
prompt, `morpheus brand status`, and the generated `hq/brand/README.md`. Written separately they
would drift, and **a prompt asking for something nothing checks looks exactly like a prompt asking
for the right thing.**

Required is short on purpose: the four files the wizard writes, plus `tokens.json`,
`visual-system.md` and `assets/logo.svg` from the session. A required list long enough to be
thorough is one nobody completes.

**Existence is not completeness.** The wizard writes an empty `tokens.json`, and an empty scaffold
beside a finished design is the failure this check exists to catch — it looks done in a listing.
So tokens must carry real values under `color`, `font` and `space`, and `visual-system.md` is
scanned for the generator's own placeholder sentences, naming *which* section was never written.

Optional entries carry a trigger rather than a deadline, and never affect the exit code.

## Why this shape

Treating an unmet trigger as a failure trains people to ignore the output. `motion.md` is not
missing before anything animates — it is correctly absent, and the trigger says so.
